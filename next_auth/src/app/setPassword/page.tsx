import SetPasswordPage from "@/components/Authentication/setPassword";
import React from "react";

type Props = {};

const SetForgotPassword = (props: Props) => {
  return <SetPasswordPage />;
};

export default SetForgotPassword;
